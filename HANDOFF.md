# 카드뉴스 에디터 — 인수인계 문서

이 문서는 **Claude Code에서 이 작업을 이어받기 위한 컨텍스트**다.
웹 채팅에서 여기까지 만들었고, 이후 작업은 이 저장소에서 진행한다.

---

## 1. 목적

ARETE(교육 스타트업) 인스타그램 카드뉴스를 만든다. 기업명 홍보 + 인사이트/정보 제공용.

기존 방식은 "요약본·컬러테마·폰트를 주고 채팅으로 수정 요청 → 결과물 받기 → 다시 수정 요청"이었다.
왕복이 너무 길어서, **문구와 요소 위치를 즉시 손볼 수 있는 편집 도구**로 전환했다.

핵심 요구는 두 가지였다.

1. 초안은 AI가 만들고, 본인 또는 팀원이 **곧바로 직접 수정**할 수 있어야 한다
2. 팀원(3명)은 디자인·협업툴 경험이 없다 → **링크만 열면 편집되는 수준**이어야 한다

---

## 2. 확정된 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| 도구 형태 | 단일 HTML 파일 에디터 | 설치·가입 없이 링크로 공유 가능 |
| 규격 | **1080 × 1350 (4:5)** | 기존 카드뉴스와 동일. 1:1 토글도 있음 |
| 콘텐츠 저장 | 카드 한 편 = JSON 파일 하나 | 디자인(HTML)과 내용(JSON)을 분리 |
| 폰트 | Gmarket Sans / MBK / Pretendard / Paperlogy / SUIT | 사용자 맥 로컬 `gang` 폴더 보유분 |
| 팔레트 | color-selector.com 화면에서 직접 지정한 **81색만** | 색 종류가 늘어나는 걸 막기 위함 |
| 배포 | **별도 공개 저장소 + GitHub Pages** | gc-system은 비공개라 무료 플랜에서 Pages 불가 |
| 협업 | JSON 파일을 저장소에 올려 주고받음 | 정적 페이지라 서버 저장이 불가능 |

### 배포를 별도 저장소로 뺀 이유
GitHub Pages는 무료 플랜에서 공개 저장소만 지원한다(Pro/Team부터 비공개 저장소 가능).
gc-system이 비공개라, 카드뉴스만 공개 저장소로 분리하기로 했다.
카드뉴스 내용은 어차피 인스타에 공개될 것이라 유출 위험이 낮다고 판단.

**주의:** 배포된 페이지 자체는 어떤 플랜에서도 공개다. 미공개 기획이 담긴 JSON은 `projects/`에 두지 않는다.

---

## 3. 파일 구조

```
<저장소>/
├─ index.html            에디터 본체. 이 파일 하나가 프로그램 전부
├─ projects/
│  ├─ index.json         편 목록. 없으면 시작 화면 없이 빈 편집기로 열림
│  └─ <편이름>.json       카드뉴스 한 편의 내용
└─ assets/
   ├─ fonts/
   │  ├─ index.json      폰트 파일명 배열. 있으면 페이지 로드 시 자동 등록
   │  └─ *.otf|ttf|woff2
   └─ img/               배경·본문 이미지
```

`projects/index.json` 형식 (문자열 배열도 허용):

```json
[
  { "id": "resilience", "title": "회복탄력성 편" }
]
```

---

## 4. 에디터 기능 현황

**편집**
- 요소 드래그 이동 / 8방향 핸들 리사이즈 / 중앙·여백 스냅 가이드
- 텍스트 더블클릭 → 캔버스에서 바로 인라인 수정 (편집 중에는 마크업 원문이 보임)
- 방향키 1px, Shift+방향키 10px 이동
- **⌘/Ctrl + ↑↓ 로 폰트 크기 조절** (Shift 병용 시 10px). 핸들 리사이즈는 글자 크기가 아니라 텍스트 상자만 바꾼다
- 카드 추가·복제·순서변경·삭제, 맨 앞/맨 뒤 레이어, 가로·세로 중앙 정렬

**인라인 마크업**
- `==문구==` → 형광펜 (배경색·글자색은 팔레트에서 자동)
- `**문구**` → 굵게

**공통 프레임** (요소가 아니라 카드 전체 설정)
- 머리말: 좌측 라벨 / 우측 `01 / 10` / 헤어라인
- 꼬리말: 로고 워터마크 / 페이지 도트 + 번호
- **페이지 번호는 카드 순서에서 자동 계산**된다. 순서를 바꿔도 알아서 맞는다

**팔레트 (핵심 로직)**
키 컬러를 최대 4개까지 고르면 역할이 자동 배정된다.
- `bg` = 명도 극단값 (밝은/어두운 토글로 반전)
- `ink` = 배경 대비가 가장 큰 색
- `hl` = 남은 색 중 채도 최고. 그 위 글자색(`onHl`)은 ink/bg 중 대비 높은 쪽 자동 선택
- `accent` = 나머지
- `sub` = ink를 bg 쪽으로 48% 혼합 (자동 파생)
- `line` = ink를 bg 쪽으로 82% 혼합 (자동 파생)
- WCAG 명암비를 실시간 계산해 4.5:1 미만이면 경고 표시

**프리셋 6종** — 표지 / 헤드+본문 / 번호 리스트 / 2단 비교 / 이미지+설명 / 결론
기존 카드뉴스 10장의 레이아웃에서 추출했다. `reference/samples/` 참고.

**입출력**
- `?p=<편이름>` → `projects/<편이름>.json` 자동 로드
- 시작 화면 목록 (`projects/index.json` 있을 때)
- 로컬 임시저장본 충돌 알림 → "내 수정본 이어서 편집 / 저장소 버전으로 계속"
- 공유 링크 복사
- PNG 내보내기 (현재 카드 / 전체), JSON 내보내기·불러오기
- 폰트 폴더 통째로 불러오기 → 파일명에서 패밀리·굵기·기울임 판별
- 폰트 목록 `index.json` 내보내기 (배포용)

---

## 5. 데이터 스키마

```jsonc
{
  "v": 3,
  "name": "회복탄력성 편",
  "size": { "w": 1080, "h": 1350 },
  "theme": {
    "picks": [ { "n": "antracite", "h": "#333333" } ],  // 최대 4개
    "dark": false,
    "font": "Pretendard"
  },
  "frame": { "label": "칼럼", "header": true, "footer": true, "logo": "" },
  "cards": [
    {
      "id": "a1b2c3d",
      "bg": { "type": "color|image", "src": "", "fit": "cover|contain",
              "tint": "#000000", "tintA": 0.35 },
      "els": [ /* 아래 세 유형 */ ]
    }
  ]
}
```

요소 공통: `id, type, x, y, w, h, opacity` (좌표는 1080×1350 기준 px)

```jsonc
// text
{ "type":"text", "text":"==훅== 문구", "size":62, "weight":800, "lh":1.32, "ls":-0.02,
  "align":"left|center|right", "valign":"start|center|end",
  "role":"ink|sub|accent|line|onAccent|bg", "color":null, "font":null, "italic":false }

// shape
{ "type":"shape", "role":"accent", "fill":null, "radius":0 }

// image
{ "type":"image", "src":"assets/img/photo.jpg", "fit":"cover", "radius":0 }
```

`color`/`fill`이 `null`이면 `role`에 따라 팔레트에서 자동 결정된다.
**색을 직접 박지 말고 role을 쓰는 것이 원칙**이다. 그래야 팔레트를 바꿀 때 전부 따라온다.
`font`가 `null`이면 `theme.font`를 따른다.

---

## 6. 이미 고친 버그 (재발 주의)

0. **`hidden` 속성이 안 먹던 것** ← 가장 오래 숨어 있던 버그
   `.picker{display:grid}` · `.bar{display:flex}` 가 `hidden` 의 브라우저 기본 스타일
   (`display:none`)을 이겨서, **편 선택 오버레이가 화면 전체를 계속 덮고 있었다.**
   82% 검은 배경이라 화면이 어두웠고, 클릭이 전부 오버레이에 먹혀 편집이 불가능했다.
   → `[hidden]{display:none!important}` 한 줄로 해결. **이 줄을 지우면 에디터가 다시 멈춘다.**

1. **`style` 속성 안의 큰따옴표**
   `font-family:"Pretendard",...` 의 따옴표가 `style="..."` 를 조기에 닫아서
   그 뒤의 `font-size`·`font-weight`·`color`가 통째로 무시됐다.
   → 폰트 이름은 **작은따옴표**로 감싼다 (`stack()` 함수).

2. **ResizeObserver 무한 루프**
   `fit()`이 캔버스 크기를 바꾸면 그게 다시 스테이지 리사이즈를 유발했다.
   → 크기가 실제로 변했을 때만 `fit()`을 호출하도록 키 비교로 차단.

3. **레이아웃 전 배율 붕괴**
   레이아웃이 잡히기 전 스테이지 높이가 0으로 읽히면 배율이 최소값으로 떨어졌다.
   → 스테이지가 60px 미만이면 `fit()`을 건너뛰고 이전 배율을 유지.

---

## 7. 알려진 한계 / 다음 과제

- **형광펜이 여러 줄에 걸칠 때** html2canvas의 `box-decoration-break` 처리가 불안정할 수 있다. 한 줄 단위 사용을 권장하되, PNG 렌더 보정이 필요할 수 있음
- **이미지가 base64로 JSON에 통째로 들어간다** → 파일이 수 MB가 된다. `assets/img/`에 올리고 경로 참조로 바꾸는 흐름을 유도해야 함
- **동시 편집 불가.** 두 명이 같은 편을 고쳐 올리면 나중 것이 앞 것을 덮는다. 운영 규칙으로 해결 중
- 로고 SVG 지원 없음
- ~~폰트 라이선스~~ **확인 완료 (2026-09-10)** — `assets/fonts/LICENSES.md` 가 정본이다.
  Pretendard·Paperlogy·SUIT 는 OFL, Gmarket Sans 는 eBay Korea 자체 라이선스. 넷 다 재배포 가능.
  **MBK 는 재배포 금지**(메르세데스 벤츠 코리아 한글체)라 저장소에 넣지 않는다 — 각자 로컬에서 불러 쓴다
- **가변폰트를 `assets/fonts/` 에 넣으면 안 된다.** `weightOf()` 가 파일명에서 굵기를 읽으므로
  가변폰트는 400 하나로만 등록된다. 굵기별 static 파일만 넣는다
- **⚠ 가짜 SUIT 저장소 주의** — `github.com/sunn-us/SUIT` 은 폰트 대신 추적 픽셀을 배포한다.
  진짜는 `github.com/sun-typeface/SUIT`. 자세한 것은 `LICENSES.md`

---

## 8. 문체 문제 (별도 과제)

초안 텍스트에서 AI 특유의 말투를 줄이고 싶다는 요구가 있다.
기존 10장을 분석한 결과 드러난 패턴:

- **"X가 아니라 Y다" 대구가 10장에 5번** — 가장 두드러지는 티. 편당 1회로 제한할 것
- **종결어미가 "~다" 하나뿐** — 질문·명령·불완전문이 없어 리듬이 평평함
- **모든 카드가 동일 골격** (제목 2줄 → 요약 1문장 → 도식 → 결론 강조 1줄)
- **추상명사 과밀** (하부구조, 기본값 환경, 구조화) — 구체적 장면·사람이 없음

**합의된 방향:** 저장소에 `voice.md`를 두고 문체 규칙(금지 구문, 좋은 예/나쁜 예)을 명시한 뒤,
초안을 뽑을 때마다 그 파일을 근거로 삼는다. 톤이 매번 대화로 재협상되지 않고 팀 자산이 된다.
→ **작성 완료: `voice.md`.** 위 패턴을 실제 10장에서 집계한 횟수, 좋은 예/나쁜 예, 편 넘기기 전 체크리스트가 들어 있다.

---

## 9. 지금 할 일

### 끝난 것 (2026-09-10)

- [x] **`voice.md` 작성** — 1편 10장 집계 기반. 금지 구문·문장 리듬·구체성·체크리스트
- [x] **폰트 라이선스 확인** — `assets/fonts/LICENSES.md`. MBK만 재배포 불가로 판정
- [x] **폰트 파일 + `assets/fonts/index.json` 업로드** — 4종 30개, woff2 변환 후 11.4MB
      Pretendard 9 · Paperlogy 9 · SUIT 9 · Gmarket Sans 3. 브라우저에서 30개 전부 로드·굵기 매핑 확인
- [x] **OFL 전문 동봉** — `*-OFL.txt` · `GmarketSans-LICENSE.txt`
- [x] **폰트 로더 지연 로딩화** — 열 때 전부 받던 것을 「쓸 때 받는다」로 바꿨다.
      39개 16.7MB 를 등록해 두지만 기본 상태로 열면 실제로 받는 것은 3~4개뿐이다
- [x] **브랜드 서체 추가** — Noto Serif KR · Cormorant Garamond 를 에디터에서 고를 수 있다.
      둘 다 가변폰트로만 배포돼 굵기별 static 인스턴스를 직접 뽑았다 (`LICENSES.md` 「다시 만드는 법」)
- [x] **첫 편 초안 — C1 「탐구 질문 좁히기」** `projects/inquiry-question.json` (7장)

### 남은 것

- [ ] **공개 저장소 생성 → push → Settings > Pages 에서 main / (root) 배포**
      로컬 git 저장소·첫 커밋·`.gitignore`·`.nojekyll` 까지는 준비됨.
      **저장소 생성과 push 는 대표의 GitHub 계정이 필요하다** (`gh` CLI 미설치)
- [ ] **첫 편 검토 → 게시** — 초안은 나왔다. **대표 확인 전에는 올리지 않는다**
- [ ] **2·3편** — D1 「상관과 인과」, A1 「AI 시대에 왜 배우나」 순 (소재 로그의 우선순위)

### 걸리는 것

- **팔레트 자동 배정으로는 1편의 색 조합이 안 나온다.** `roles()` 는 `hl`(형광펜)을
  「남은 색 중 HSV 채도 최고」로 정한다. 1편은 *연두 형광펜 + 주황 포인트* 였는데,
  웬만한 주황은 연두보다 채도가 높아서 **주황이 형광펜을 가져간다.**
  첫 편은 포인트를 채도 낮은 `green gold #ABA76F` 로 써서 우회했다.
  1편 그대로 재현하려면 `roles()` 를 고치거나 요소마다 색을 직접 박아야 한다(원칙에 어긋난다)

- **`index.html` 프리셋 6종의 예시 문구가 1편 원문 그대로다.** 그 안에
  "강한 멘탈은 타고나는 것이 아니라 구조 속에서 길러진다", "회복탄력성을 갉아먹는 기본값 환경" 처럼
  **`voice.md` 가 금지한 구문이 들어 있다.** 프리셋을 그대로 쓰면 초안이 다시 그 말투로 시작된다.
  예시 문구를 교체하거나, 아예 뜻 없는 자리표시자로 바꾸는 편이 낫다
